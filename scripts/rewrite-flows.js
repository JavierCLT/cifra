const fs = require('fs');
const path = 'public/js/render-tables.js';
let text = fs.readFileSync(path, 'utf8');

const startMarker = 'function renderHeadcountFlowsTable';
const endMarker = '\n\n// Render headcount tab';
const start = text.indexOf(startMarker);
if (start === -1) throw new Error('renderHeadcountFlowsTable not found');
const end = text.indexOf(endMarker, start);
if (end === -1) throw new Error('end marker not found');

let replacement = `function renderHeadcountFlowsTable(data, months, opts = {}) {\n    if (!data || AppState.isGroupView) {\n        return '';\n    }\n\n    const DEFAULT_FLOW_ROWS = [\n        { key: 'starting_headcount', label: 'Starting Headcount' },\n        { key: 'flow_1', label: 'Flow 1' },\n        { key: 'flow_2', label: 'Flow 2' },\n        { key: 'flow_3', label: 'Flow 3' },\n        { key: 'flow_4', label: 'Flow 4' },\n        { key: 'flow_5', label: 'Flow 5' },\n        { key: 'ending_headcount', label: 'Ending Headcount', isCalculated: true }\n    ];\n\n    const flowRows = Array.isArray(window.HEADCOUNT_FLOW_ROWS) && window.HEADCOUNT_FLOW_ROWS.length\n        ? window.HEADCOUNT_FLOW_ROWS\n        : DEFAULT_FLOW_ROWS;\n\n    const forecastStatus = data.forecastStatus || {};\n    const flows = data.headcountFlows || {};\n\n    let html = '<div class="data-table-wrapper headcount-flow-wrapper"><table class="data-table headcount-flow-table">';\n    html += '<thead><tr><th rowspan="2">Flow</th>';//MARKER1\n\n    months.forEach((month, idx) => {\n        const isForecast = forecastStatus[month] === 'Forecast';\n        const businessDays = window.BUSINESS_DAYS?.[idx] || 21;\n        html += '__DOLLAR__{`<th class="__DOLLAR__{isForecast ? 'forecast-col' : 'actual-col'}">__DOLLAR__{businessDays}</th>`}';\n    });\n\n    QUARTERS.forEach(() => {\n        html += '<th class="quarter-col">Avg</th>';//MARKER2\n    });\n\n    YEARS.forEach(year => {\n        html += '__DOLLAR__{`<th class="year-total-col">FY__DOLLAR__{year.slice(-2)}</th>`}';\n    });\n\n    html += '</tr><tr>';//MARKER3\n\n    months.forEach(month => {\n        const isForecast = forecastStatus[month] === 'Forecast';\n        html += '__DOLLAR__{`<th class="__DOLLAR__{isForecast ? 'forecast-col' : 'actual-col'}">__DOLLAR__{month}</th>`}';\n    });\n\n    QUARTERS.forEach(quarter => {\n        html += '__DOLLAR__{`<th class="quarter-col">__DOLLAR__{quarter}</th>`}';\n    });\n\n    YEARS.forEach(() => {\n        html += '<th class="year-total-col">Avg</th>';//MARKER4\n    });\n\n    html += '</tr></thead><tbody>';//MARKER5\n\n    flowRows.forEach(row => {\n        const rowValues = flows[row.key] || {};\n        html += '__DOLLAR__{`<tr><td>__DOLLAR__{row.label}</td>`}';\n\n        months.forEach(month => {\n            const isForecast = forecastStatus[month] === 'Forecast';\n            const baseClass = isForecast ? 'forecast-col' : 'actual-col';\n            const value = Number(rowValues[month] ?? 0);\n            html += '__DOLLAR__{`<td class="__DOLLAR__{baseClass}">__DOLLAR__{value}</td>`}';\n        });\n\n        QUARTERS.forEach(quarter => {\n            const avg = calculateQuarterAverage(rowValues, quarter);\n            html += '__DOLLAR__{`<td class="quarter-col">__DOLLAR__{avg}</td>`}';\n        });\n\n        YEARS.forEach(year => {\n            const avg = calculateYearAverage(rowValues, months, year);\n            html += '__DOLLAR__{`<td class="year-total-col">__DOLLAR__{avg}</td>`}';\n        });\n\n        html += '</tr>';//MARKER6\n    });\n\n    html += '</tbody></table></div>';//MARKER7\n    return html;\n}\n`;

replacement = replacement.replace(/__DOLLAR__/g, '$');
replacement = replacement.replace(/\/\/MARKER[0-9]+/g, '');

const newText = text.slice(0, start) + replacement + text.slice(end);
fs.writeFileSync(path, newText);
